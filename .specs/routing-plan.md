## Routing Plan
Generated: 2026-07-08 · Updated: 2026-07-10 (all specs shipped+deployed; batch complete)

- ~~.specs/01-unit-hint-display.md~~ — MERGED + LIVE 2026-07-09 (codex-generated, Tier A committed the verified diff after codex exited without committing; Opus-reviewed clean).
- ~~.specs/02-load-ratchet.md~~ — MERGED + LIVE 2026-07-09 (Tier A direct after dispatch-layer failure; Opus-reviewed).
- ~~.specs/03-ht-load-override-fix.md~~ — MERGED + LIVE 2026-07-09 (Tier A direct after dispatch-layer failure; Opus-reviewed, HIGH finding fixed pre-merge).
- ~~.specs/04-ramp-sets.md~~ — MERGED + LIVE 2026-07-10 (codex-generated; Opus-reviewed clean, one non-blocking Low noted).
- ~~.specs/05-finisher-emom.md~~ — ABANDONED as a single dispatch (branch renamed `abandoned/task-05-finisher-emom`): 2× gemini timeout (zero output) + 1× codex partial (correct but incomplete, 3 files, no commit) — too large for one generation pass. Decomposed into 05a/05b/05c.
- ~~.specs/05a-finisher-schema.md~~ — MERGED 2026-07-10 (codex-generated, Tier A committed after codex again exited uncommitted; Opus-reviewed clean).
- ~~.specs/05b-finisher-progression-rule.md~~ — MERGED 2026-07-10 (codex-generated, Tier A committed after codex again exited uncommitted; Opus-reviewed clean, two non-blocking Low notes: window=1-only test coverage, minor _ladder_step re-derivation).
- ~~.specs/05c-finisher-generation.md~~ — MERGED + LIVE 2026-07-10 (two codex dispatches: initial + a fix for a caught-by-tests `_serialize_session` signature mismatch, both left uncommitted, both committed by Tier A after verification; Opus-reviewed clean, two non-blocking Low notes: legacy-session degradation, rest-day signature imprecision — both cosmetic under current design).

All 8 specs in this batch (01-03 from the prior session, 04/05a/05b/05c from this one) are MERGED and LIVE as of 2026-07-10. Deploy required an extra step beyond the CLAUDE.md Deploy Gate's restart procedure: migrations 025/026 only added schema, so a new one-off idempotent seed script (`ironlog/generation/live_seed_ramp_and_finishers.py`) was written, tested against a DB copy, and run against production (human-gated per Class 2, explicit confirmation obtained) to actually populate the ramp-eligible flags and finisher rows.

## New spec (2026-07-10, post-deploy incident)
- ~~.specs/06-fallback-replay-slot-identity.md~~ — MERGED + LIVE 2026-07-10 (codex-generated, Opus-reviewed clean). Fixed a real bug found live: `last_valid_selections` zips current skeleton slots against a prior session's exercises by raw position, silently defeating any exercise-order or meso-rotation change once a deviation signal routes generation through the fallback/replay path.
- ~~.specs/07-giant-set-knee-modality-grouping.md~~ — **RETRACTED, not merged** (see the spec file's own retraction note). Misdiagnosed a genuine program-design inconsistency (D5's T3 tier seeds 4 concurrent exercises against the validator's 3-max GIANT_SET_CONCURRENCY room-geometry rule) as a code bug. Implementing the spec as written broke 8 pre-existing tests. Branch abandoned (`abandoned/task-07-giant-set-grouping`). Needs a program-design decision from the user before any code changes, not a code fix.
- ~~.specs/08-giant-set-cap-raise-and-grouping-fix.md~~ — MERGED + LIVE 2026-07-10 (codex-generated, Opus-reviewed clean). Corrected/combined version of spec 07: raises `GIANT_SET_CONCURRENCY` 3→4 (athlete directive) alongside the grouping fix, so the fix doesn't reject at the validator.
- ~~.specs/09-assist-ladder-seed-source-gap.md~~ — MERGED + LIVE 2026-07-10 (codex-generated, Tier A committed after codex again exited uncommitted; Opus-reviewed clean, two non-blocking Low notes). Closes the `assist_ladder` seed-source gap for Nordic Curl / Nordic Curl - Volume / Reverse Nordic Curl; Pull-up deliberately untouched.
- `.specs/10-ai-acts-on-notes-scoping.md` — **scoping doc, NOT a dispatch-ready spec.** Covers build-plan item H ("AI acts on programming notes"). Captures what already exists (note classification, manual apply, the attachment-vs-subject safety gap) and the open design questions (most importantly: should anything ever apply without per-instance human confirmation) that need a real brainstorming session before any implementation spec is written. Not in this repo's dispatch queue.
- ~~.specs/11-warmup-generation.md~~ — MERGED + LIVE 2026-07-12 (codex-generated, Tier A committed after codex exited uncommitted; Opus-reviewed clean). Closes the `/generate` scope marker's own admitted gap. Migration 027 applied to production + `live_seed_warmup.py` run, both human-gated per Class 2 (explicit confirmation obtained at dispatch, merge, and production-apply steps separately). Smoke-checked live for D6.
- `.specs/12-weak-point-hints-day-scoping.md` — **dispatch-ready** (2026-07-12). Real bug: `build_weak_point_hints(db)` queries ALL `MovementState` rows and keys its result by `movement_id` alone, but `MovementState` has a genuine composite `(movement_id, day_id)` key (a movement like Hip Thrust has one row per day-track D2/D5/D6) — last-write-wins-by-iteration-order silently picks an arbitrary day's stall signal for every day's generation. Fix threads `day_id` (= `day_role`, already in scope at the call site) through, with exact-match-then-legacy-null-fallback resolution mirroring `_resolve_movement_state`'s pattern (read-only, no mutation). Soft-signal-only (L1 hint), Option-C/load-writing paths untouched. No schema change, no HUMAN GATE.

(Meadows Row's +1.25lbs note was applied directly via the existing LOAD-override mechanism — no new code needed, same as the Hip Thrust case. Not a spec item.)

**Client-side follow-on**: build-plan items G (autoregulated rest) + D+E (background rest timer) were specced together in `~/projects/IronLog-V2-Client/.specs/01-autoregulated-background-rest-timer.md` (a separate repo — see that repo's own routing plan). Not dispatched from here.

## 2026-07-12 batch: "H — AI acts on programming notes" (post-brainstorming)

Design approved via brainstorming session 2026-07-12: **nothing auto-applies** — the AI resolves a note into a fully-specified, safety-checked proposal for one-tap human confirmation, covering all three build-plan categories (reorder / assist+reps / cross-day) through one general propose→confirm→apply mechanism.

- `.specs/13-classifier-reorder-action-type.md` → codex, worktree wt-13, depends on: none. Adds `REORDER` action_type + structured `before_movement`/`after_movement` extraction to `NoteClassifier`. Bounded (1 file + tests).
- `.specs/14-reorder-override-type.md` → codex, worktree wt-14, depends on: none (parallel-safe with 13 — no shared files). Adds `OverrideType.REORDER` + `SlotMovementOverride.override_order` (migration 028) + `apply_override`/`ApplyNoteRequest` wiring. **DB SCHEMA CHANGE — HUMAN GATE at merge and at production deploy**, per CLAUDE.md's Forbidden list (no additive-schema carve-out at the orchestration level, only in the migrations/README's file-authoring convention).
- `.specs/15-skeleton-honors-reorder-override.md` → codex, worktree wt-15, depends on: 14 merged (needs `OverrideType.REORDER`/`override_order` to exist). `lay_skeleton` effective-order sort.
- `.specs/16-note-resolver.md` → gemini, worktree wt-16, depends on: 13 AND 14 merged. The core new module — broader/higher-stakes than the other specs (candidate-finding, ranking, per-action-type override computation, pre-commit safety check against the exact HT-clamp near-miss scenario) — gemini per "broad cross-module, higher-stakes" guidance, and this one gets an Opus review regardless of how clean the diff looks (touches a safety-critical validation boundary).
- `.specs/17-notes-review-resolved-proposals.md` → codex, worktree wt-17, depends on: 16 merged. Wires the resolver into `/notes/review`.

Delegation ratio: 5/5 → codex/gemini (100%)
Merge order: (13, 14 in parallel) → 15 → 16 → 17. **13/14 run concurrently (worktree-isolated, no shared files) but merge serially** per the standing serial-merge rule — whichever finishes review first merges first, the other rebases if `main` moved.

Notes:
- **This is a genuinely higher-stakes batch than prior sessions' work** — 16 (the resolver) computes overrides that a human will one-tap-confirm without necessarily re-deriving the math themselves, and its pre-commit safety check is the whole reason this design doesn't repeat the HT-clamp near-miss. Route 16 through Opus review unconditionally, do not classify it review-exempt under any circumstance, and hold the bar high on the HT-clamp-fixture test specifically — that test failing to catch a broken safety check is the single worst outcome this whole design exists to prevent.
- Client-side follow-on: `IronLog-V2-Client/.specs/10-apply-wizard-resolved-proposals.md` — depends on spec 17 merged + live (verify the real `resolved_proposals` shape via a live `curl` before dispatching, same pattern as the warmup/finisher client follow-ons earlier this session).
- Item H's other two named categories (assist+reps, cross-day) are NOT separate specs — they're covered by the SAME general mechanism (13/14/16/17), differentiated only by `action_type` inside the resolver. Only reorder needed new schema/model support.

## 2026-07-17 addendum: HT band-peak refinement over-drift bug

Athlete-reported live discrepancy: D5 HT displayed 170lb plates + Red band = 263.2lb felt, when Red's real peak should read ~90lb. Traced to a genuine bug in `ht_refine.py`'s single-band felt-peak refinement (Task 5 of the original HT band-composite plan): the docstring promises "multiple qualifying sets in the same session collapse to ONE observation," but the actual EMA-update loop applies the EMA once per qualifying `SetLog`, not once per session — a 3-set HT exercise compounds a single session's nudge threefold. Confirmed by replaying the live session-8 data (3× felt_peak=260/plates=165) against the buggy loop, landing almost exactly on the observed drifted value (93.2 vs the seed default 90.0).

- `.specs/20-ht-refine-per-session-not-per-set.md` → codex, worktree wt-20, depends on: none. Restructures the EMA loop to collapse same-session/same-band logs to one mean observation before applying the EMA (reusing the collapsing logic `_single_band_session_observations` already implements for its own consistency-gate purpose). New test file `tests/test_ht_refine.py` — **no test coverage existed for this file at all before this spec.** Real logic fix, not mechanical — routed through Opus review before merge.
- Live data correction (separate from the code fix, applied directly after the code fix is verified): reset `BandPair` id=2 (Red)'s `peak_lb` from the drifted 93.2 back to the value the CORRECTED logic would have produced by replaying real history (91.5, not a guessed round number) — recorded as a direct DB mutation with the replay math shown in the commit message, same pattern as this session's other direct data corrections.

Delegation ratio: 1/1 (100%)
Merge order: wt-20 standalone.

Notes for future batches:
- **Recurring pattern this session (6 occurrences across specs 04/05a/05b/05c, both providers): a worker produces correct, fully-tested work but exits without running `git commit`.** AGENT.md/GEMINI.md were already strengthened mid-session ("finished = committed" as a hard criterion) but the recurrence continued afterward — this may be a turn/token budget cutting workers off right after their test run, not a prompt-compliance issue. Worth investigating the underlying dispatch mechanism (consensus-mcp) rather than the instruction text.
- **Combined/large specs should be decomposed into single-dispatch-sized sub-specs up front**, not after a failed attempt — spec 05's original combined form cost 3 failed dispatch attempts before decomposition fixed it. When a spec touches more than ~3-4 files across schema + engine + API layers, split it at those natural seams before the first dispatch.
- **Schema migrations and their seed data are not the same deploy step.** A migration file only changes structure; if a feature also needs new rows (not just new columns), that's a separate, explicitly-gated data action — don't assume "the migration ran" means "the feature is live." This cost one round of a smoke-check catching stale data mid-deploy.
- Spec 01 has an out-of-repo client follow-on (IronLog-V2-Client render change) — not yet spec'd.

## 2026-07-18 batch: recovery/readiness check-in (design approved via brainstorming, docs/superpowers/specs/2026-07-18-recovery-readiness-checkin-design.md)

Makes the STAB->REBUILD phase gate real for the first time — it and its bodyweight-driven CUT->STAB counterpart have been fully inert end-to-end (EngineStateInput has only ever been constructed with current_phase set, every other field left at its dataclass default).

- ~~.specs/21-daily-readiness-model.md~~ → codex, worktree wt-21 — MERGED + LIVE 2026-07-18 (77e27fb). New `DailyReadiness` table + migration 029. Opus review: zero Critical/High/Medium, one harmless Low (index-naming, outside the migration-parity invariant by design). One scope violation caught and fixed pre-commit: codex's first attempt renamed a shared `date` import and touched two unrelated pre-existing fields (`BandPair.inspection_date`, `MovementState.rep_scheme_locked_until`) to work around a real Pydantic v2 constraint — reverted to a narrowly-scoped local alias instead. **[HUMAN GATE] satisfied** — schema-change merge explicitly authorized separately from the earlier generation go-ahead.
- ~~.specs/22-readiness-compute-functions.md~~ → codex, worktree wt-22 — MERGED + LIVE 2026-07-18 (21eeaa3). Opus review (2 passes): first pass found a **CRITICAL** bug (`_trailing_rows` anchored its window on the data's own latest row instead of the actual current date, so weeks-stale check-in data could silently pass the STAB→REBUILD gate) — fixed by requiring an explicit `as_of: date` parameter on every date-windowed function; re-review confirmed the fix empirically (not just by reading the diff) with no new issues. Rebased cleanly onto main after spec 21 merged (zero conflicts, disjoint files).
- ~~.specs/23-readiness-wiring-and-endpoints.md~~ → gemini, worktree wt-23 — MERGED + LIVE 2026-07-18 (3e2f30a). Wires real data into `run_analysis.py`'s `EngineStateInput` construction + new `GET /readiness/today`, `POST /readiness`, `POST /engine-state/confirm-phase` endpoints; `phase_transition_available` now threaded through `SubmitResponse` (previously discarded). New `EngineState.pending_phase_transition` column + migration 030 (**undeclared schema addition, not in the original spec's file targets — [HUMAN GATE] separately authorized at dispatch-fixup time and again at merge**, matching the two-gate pattern spec 21 established). Opus review (unconditional, per this batch's own routing note): current_phase write-invariant holds (confirm-phase is the sole write site), confirm-phase can't be spoofed with an unoffered transition, partial-upsert correctly preserves omitted fields, both new integration tests trace to real threshold math. One **Medium** (RHR baseline window coded 7-90 days instead of the spec'd 60-90, contaminating the long-run baseline with semi-recent readings) — fixed as a one-line literal-value change (Tier A direct, exception #3) and re-verified against the full suite rather than re-reviewed (trivial, test-caught fix, per Review Gate's "gate on the fix's risk, not the finding's tier"). Two Low notes, not blocking: idempotent-submit-retry drops `phase_transition_available` from the response (DB state itself is fine); RPE-creep window is count-bounded (last 3 PROGRESS rows) not date-bounded. Two gemini dispatches: initial implementation (uncommitted, Tier-A-verified-then-committed per exception #7), then a fixture fix for `tests/test_run_analysis_phase_gate.py`'s NOT NULL violations (committed by gemini itself this time).

Delegation ratio: 3/3 (100%)
Merge order: (21, 22 in parallel, worktree-isolated, no shared files) → 23. 21/22 merged serially per the standing rule even though generation was concurrent — 21 merged first, 22 rebased cleanly (zero conflicts, disjoint files) — then 23 merged once both were live.

**All 3 specs in this batch are MERGED and LIVE as of 2026-07-18 (main @ 3e2f30a, 563 tests passing). Recovery/readiness check-in feature is server-complete.**

Notes:
- Client follow-on (IronLog-V2-Client): a daily check-in card + confirm-phase-transition surface, per the design doc §5. **Not specced yet** — now unblocked (spec 23 is merged + live); verify the real endpoint shapes (`DailyReadinessOut`, `GET /readiness/today`, the confirm-phase request/response, and `SubmitResponse.phase_transition_available`) via a live curl before dispatching, matching this session's established pattern for client follow-ons on server-first features.
- Polar Verity Sense / Samsung Watch device-sync integration is explicitly out of scope for this whole batch (see design doc's "Explicitly out of scope") — the `bodyweight_source`/`resting_hr_source` fields on `DailyReadiness` (spec 21) are the seam a future spec plugs into, not something this batch builds.
- **Deploy note**: this batch shipped a schema change (migration 029 + 030) to the live IronLog-V2 server. Restart is a **Class 2** (schema/data migration) per the Deploy Gate — always HUMAN GATE, not autonomous. Not yet restarted/deployed to `ironlogv2.service` as of this merge; the running service is on the pre-batch code until that gate is cleared.
- **Process note**: the two "gemini reports timed out but the underlying `agy` process is alive and working" incidents in this batch (both spec-23 dispatches) followed the established timeout playbook exactly — found the live PID via `ps aux`, polled for natural exit rather than re-dispatching into a worktree with a possibly-still-running process, and both resolved correctly (one produced a complete uncommitted diff, one committed on its own).
- **Process note**: gemini's spec-23 diff introduced an out-of-target-list schema change (`EngineState.pending_phase_transition`) without a migration. Caught by the mandatory scope check before review, not after — worth remembering that "file targets" in a spec constrain what's *authorized*, and a worker deviating from that list (even for a reasonable design reason the spec itself speculated about) is a genuine gate trigger, not a rubber-stamp.

## 2026-07-18 batch #2: Withings body-scan integration (design approved via brainstorming, docs/superpowers/specs/2026-07-18-withings-body-scan-integration-design.md)

Syncs bodyweight + body-fat % from a Withings body-composition scale into `DailyReadiness`, via a real public webhook (myflix.media's Traefik route is genuinely internet-reachable, not just LAN cert theater) with a nightly reconciliation timer as a backup net. Deliberately excludes resting HR (unreliable on this athlete's scale) and weight/body-fat **goals** (would touch the CUT→STAB phase gate's hardcoded `cut_to_stab_target=213.0` — split into its own separate design per the brainstorming session's decomposition call, not built here).

- ~~.specs/24-withings-credentials-model.md~~ → codex, worktree wt-24 — MERGED + LIVE 2026-07-18 (cc638ab). New `WithingsCredentials` singleton table + migration 031. Review-exempt (additive-only schema, spec-dictated tests, logged per Review Gate). 565 passed. **[HUMAN GATE] satisfied at dispatch and merge.**
- ~~.specs/25-daily-readiness-body-fat.md~~ → codex, worktree wt-25 — MERGED + LIVE 2026-07-18 (802dbc5). Resequenced after spec 24 (both target `ironlog/models/library.py` — originally marked parallel-safe, corrected by `/verify-plan`). Adds `DailyReadiness.body_fat_pct` + migration 032. Review-exempt. 566 passed. **[HUMAN GATE] satisfied at dispatch and merge.**
- ~~.specs/26-withings-oauth-flow.md~~ → codex, worktree wt-26 — MERGED + LIVE 2026-07-18 (see main log). New `ironlog/integrations/` package, OAuth2 authorize/callback endpoints + token exchange/refresh helpers. **Opus review (unconditional, touches auth/secrets): clean of Critical/High.** One **Medium filed as an open follow-up, not fixed**: the OAuth2 `state` CSRF param is generated (`_uuid.uuid4().hex`) but never persisted or validated on `/callback` — a forged callback with an arbitrary `code` could complete against this endpoint and overwrite `WithingsCredentials(id=1)`. Deferred rather than blocked: fixing it needs new session/nonce infrastructure this single-user LAN app doesn't otherwise have, and the practical threat model requires an attacker to already have host-level network access. **Open item for a future hardening pass if this server is ever exposed beyond the current LAN+Traefik-webhook-only posture.** Two Low notes (no graceful consent-denied path; non-numeric `expires_in` would 500 not 502), cosmetic, left as-is. 574 passed. **[HUMAN GATE] satisfied at dispatch and merge.**
- ~~.specs/27-withings-sync-logic.md~~ → gemini, worktree wt-27 — MERGED + LIVE 2026-07-18 (see main log). Core cross-module logic: Measure API client, unit/exponent decoding, kg→lb conversion, upsert reconciliation. **Opus review (unconditional, highest-risk spec in this batch) found 1 HIGH**: a refreshed OAuth token pair was discarded on transaction rollback if the Measure API call failed right after a token refresh — since Withings rotates refresh tokens, an ordinary transient network blip would have permanently broken sync. Re-delegated to gemini (commit the refresh immediately, before the failable call); Medium (`days_updated` over-counting) folded into the same fix. **Re-review empirically confirmed the fix survives a fresh-session boundary** (not just diff-read) via a standalone two-session verification script — clean GO. The fat-ratio (type 6) percentage-vs-fraction convention flagged as unverifiable from code alone was confirmed directly by the user against the Withings developer portal (decodes to a percentage, matching the code). 582 passed. No HUMAN GATE required (no Forbidden-list hit for this spec).
- ~~.specs/28-withings-nightly-timer.md~~ → codex, worktree wt-28 — MERGED + LIVE 2026-07-18 (see main log). Standalone `scripts/sync_withings.py` + systemd timer/service (03:00 daily, mirrors Flixd's timer pattern). Review-exempt (pure deploy-infra, no application logic). Confirmed `EnvironmentFile` against the LIVE production `ironlogv2.service` unit file on myflix, which has it — the repo's checked-in copy of `deploy/ironlogv2.service` is missing that line, a real drift worth reconciling separately (not fixed here, out of scope). 582 passed, no regressions. No HUMAN GATE required.
- ~~.specs/29-withings-webhook-and-manual-trigger.md~~ → codex, worktree wt-29 — MERGED + LIVE 2026-07-18 (see main log). New `POST /integrations/withings/webhook` (public) + `POST /integrations/withings/sync-now` (LAN-only). First iteration worked around a missing `python-multipart` dependency with a 40-line runtime fallback parser; simplified after human authorization to add the dependency instead. **Opus review found 1 HIGH**: the reference Traefik router (`deploy/traefik/withings-webhook.yml`) used a `Host()`-only rule with no path constraint, which would have exposed the ENTIRE unauthenticated API to the internet, not just the webhook — including the OAuth callback and every state-mutating endpoint. Fixed directly (Tier A, exception #3 — single-line config addition, `&& Path(...)`, to a reference-only file not itself application code). **Re-review confirmed the exact-match `Path()` scoping genuinely closes the exposure** (verified Traefik matcher semantics, not assumed). One Medium (sync-now's error-status mapping is a substring match on error text) partially closed — tests now pin byte-exact production error strings instead of fabricated stand-ins; the underlying substring-match fragility itself is filed as an open follow-up. 586 passed. **[HUMAN GATE] satisfied at dispatch and merge.**

**All 6 specs in this batch are MERGED and LIVE as of 2026-07-18/19 (main @ 0fd03b8, 586 tests passing). Withings body-scan integration is server-complete.**

**DEPLOYED AND FULLY LIVE as of 2026-07-19**: migrations 031/032 applied to production, `ironlogv2.service` restarted, `ironlog-withings-sync.timer` installed and enabled (03:00 daily). DNS for `withings.myflix.media` already existed (wildcard, same zone as `plex`/`admin`/`core`) — no new record needed. Traefik reference config copied to `~/server-ops/apps/traefik/dynamic/withings-webhook.yml`, picked up automatically (`watch: true`). **Discovered during deploy and fixed**: the Traefik route as originally scoped (webhook path only) 404'd on `/authorize` — the OAuth flow genuinely needs `/authorize` and `/callback` (Withings' registered redirect_uri) reachable publicly too, not just the webhook. Widened the route to an explicit OR of 3 exact paths (webhook/authorize/callback), deliberately NOT a `PathPrefix()` match since `/integrations/withings/sync-now` shares the same URL prefix but must stay LAN-only — human-authorized before applying (same Forbidden-list category as the original exposure). One-time OAuth browser authorization completed by the user; `WithingsCredentials(id=1)` populated. **A real manual sync then succeeded end-to-end**: `POST /sync-now` returned `{"days_updated": 2, "measurements_fetched": 4}`, and `DailyReadiness` now has real Withings-sourced rows (confirmed via direct DB read: bodyweight + body_fat_pct populated, `bodyweight_source="withings"`, `resting_hr` correctly left untouched). Live evidence from Traefik's access log also incidentally confirmed the original HIGH-severity Path-scoping fix holds under real internet traffic — an automated vulnerability scanner hitting `withings.myflix.media` with paths like `/wp-config.php`, `/.env.production`, `/database.sql` got a 404 on every one, since none match the router's scoped path list.

Delegation ratio: 6/6 (100%)
Merge order: 24 → 25 → 26 → 27 → (28, 29 in parallel — both depended only on 27/26+27, no shared files between them, confirmed disjoint by `/verify-plan`, and both merged cleanly with no rebase conflicts).

## Open follow-up items from this batch (not built, not blocking, tracked here per Deploy Gate discipline)
- **OAuth CSRF gap (spec 26)**: the `state` param is generated but never validated on `/callback`. Real gap, narrow threat model (single-user LAN app, attacker needs pre-existing host network access). Revisit if this server's exposure ever broadens beyond the current LAN + single Traefik-routed webhook path.
- **sync-now error-mapping fragility (spec 29)**: `status_code = 400 if "not yet authorized" in str(exc) else 502` is a substring match on `sync_withings_measurements`'s error text. Tests now pin the exact production strings (closing the "silent breakage" risk), but the coupling itself remains — a cleaner fix would be a typed exception (e.g. a `WithingsNotAuthorizedError` subclass of `RuntimeError`) instead of message-matching. Small blast radius (single-user app), not urgent.
- **`deploy/ironlogv2.service` repo/live drift (spec 28 discovery)**: the live unit file on myflix has `EnvironmentFile=-/home/jstout/projects/IronLog-V2/.env`; the repo's checked-in copy doesn't. Should be reconciled so the repo is the source of truth again, but this batch's new timer/service files correctly matched the LIVE config, so nothing is currently broken.

Notes:
- **Spec 27 is the highest-risk piece of this batch** — it's the only one doing real external-API integration logic (unit/exponent decoding, a wrong conversion silently corrupts bodyweight data going into the same STAB→REBUILD gate the whole recovery/readiness feature exists to make trustworthy). Route through Opus review unconditionally regardless of how clean the diff looks, same standard as spec 23 and the note-resolver spec (16) in earlier batches.
- **Four of six specs (24, 25, 26, 29) require explicit HUMAN GATE authorization at worktree-creation/dispatch time, separately again at merge** — same two-gate pattern established for spec 21 in the prior batch. Only specs 27 and 28 can proceed on standing autonomous authorization once their dependencies clear.
- **This batch's actual deploy has real infra steps outside this repo's control**: a DNS record for `withings.myflix.media`, copying the Traefik dynamic-config file to `~/server-ops` on myflix, and the one-time manual OAuth browser authorization. None of these can be automated from within this repo's dispatch/merge pipeline — they're deploy-time human steps, called out explicitly in specs 28/29's own Verification sections.
- Client-side follow-on: none identified yet — this integration is server-only (no new client-visible data beyond what already displays via `DailyReadiness`/`GET /readiness/today`, unless a future trend view wants to surface `body_fat_pct` specifically).

## 2026-07-19 batch: goal-driven phase gate thresholds (design approved via brainstorming, docs/superpowers/specs/2026-07-19-goal-driven-phase-gate-thresholds-design.md)

Replaces the CUT→STAB phase gate's hardcoded `cut_to_stab_target=213.0`/`tolerance=2.0` with a real, athlete-settable `GoalSettings` table. Expanded mid-brainstorming: body-fat % becomes a load-bearing OR-alternative to the weight goal (not just storage), gated by a new week-long stability check that rejects a transient dip-then-rebound from firing the gate early. Deferred from the Withings body-scan integration batch since it touches the phase gate, a stated engine invariant.

- ~~.specs/30-goal-settings-model.md~~ → codex, worktree wt-30 — MERGED + LIVE 2026-07-19 (9616899). New `GoalSettings` singleton table + migration 033 (schema-only, no data seed). Review-exempt (additive-only, spec-dictated tests). 588 passed. **[HUMAN GATE] satisfied at dispatch and merge.**
- ~~.specs/31-compute-goal-stable.md~~ → codex, worktree wt-31 — MERGED + LIVE 2026-07-19 (5d8f57a). New `compute_goal_stable` pure stability-check function serving both goal types via a `field` parameter; also fixed a real pre-existing gap (`DailyReadinessInput` was missing `body_fat_pct`). Routed through Opus review despite no explicit mandate (non-trivial new logic, not just additive schema) — clean of Critical/High/Medium, two Low notes. 593 passed. No HUMAN GATE required.
- ~~.specs/32-goal-gate-wiring.md~~ → gemini, worktree wt-32 — MERGED + LIVE 2026-07-19 (a0bf991). Real gate-logic change: `EngineStateInput`'s shape changed (`bodyweight`/`cut_to_stab_target`/`cut_to_stab_tolerance` removed, `weight_goal_stable`/`body_fat_goal_configured`/`body_fat_goal_stable` added), CUT→STAB branch now an OR of two week-long stability checks. Also fixed a second real gap: `readiness_inputs`'s DB-row mapping never carried `body_fat_pct` through at all, which would have silently broken the body-fat goal regardless of real data. **Opus review (unconditional, this batch's designated highest-risk spec): clean of Critical/High.** One Medium, documentation-precision only, no code fix: the no-`GoalSettings`-row fallback preserves the historical 213.0/2.0 *thresholds* but not the exact old *decision semantics* — the old gate fired on a single qualifying reading, the new fallback (like every other path) requires a sustained week. Deliberate and strictly safer (the gate is report-only, never auto-applies), but the original design doc's "zero behavior change on deploy" framing was imprecise — recorded explicitly rather than left as a silent divergence. Two Low notes (test naming drift, an optional stricter-fallback regression test), cosmetic. 597 passed. No HUMAN GATE required.
- ~~.specs/33-goals-endpoint.md~~ → codex, worktree wt-33 — MERGED + LIVE 2026-07-19 (6d03693). `GET`/`POST /goals`, mirrors the established `POST /readiness` upsert pattern exactly. Review-exempt (mirrors an already-reviewed pattern, no novel logic). 601 passed. No HUMAN GATE required.

**All 4 specs in this batch are MERGED and LIVE as of 2026-07-19 (main @ 6d03693, 601 tests passing). Goal-driven phase gate thresholds feature is server-complete.**

Delegation ratio: 4/4 (100%)
Merge order: 30 → 31 → 32 → 33 (30/31 were parallel-safe by design but ended up merging serially in practice; 32/33 likewise — no conflicts at any point, confirmed disjoint files throughout).

Notes:
- **Spec 32 was this batch's highest-risk piece as predicted** — it changed `EngineStateInput`'s shape and the gate's actual decision logic, not just its data source. Opus review confirmed correctness (clean of Critical/High) and caught the second `body_fat_pct`-wiring gap (in `readiness_inputs`'s mapping) before merge — the same category of silent-data-gap risk the Withings batch's HIGH findings warned about.
- **Only spec 30 required a HUMAN GATE** (DB schema change) — specs 31, 32, 33 hit no Forbidden-list item and proceeded on standing autonomous authorization.
- **A genuine behavior-semantics nuance surfaced during spec 32's review**, worth remembering for future gate-touching work: "preserving the historical constants" (thresholds) is not the same claim as "preserving identical decision behavior" (semantics) — a stricter, sustained-state check can technically change when a report-only gate fires even while keeping the same threshold numbers. Safe here because the gate never auto-applies, but the distinction matters more if a future gate ever does.
- Client-side follow-on: none yet — server-only, no new client-visible surface beyond `GET /goals` (which nothing currently displays).

## 2026-07-19 batch: real weak-point assessment (design approved via brainstorming, docs/superpowers/specs/2026-07-19-weak-point-assessment-design.md)

First of the three remaining Phase-1 roadmap items (missed-workout handling and a standalone cardio/interval day-type designed separately, no dependency between the three). New athlete-visible surface — distinct from the existing internal `build_weak_point_hints` (a silent per-movement generation nudge, untouched by this batch). Combines the existing `detect_stall` with a new relative-growth-rate comparison against the athlete's own median across other movements.

- ~~.specs/35-growth-rate-lagging-functions.md~~ → codex, worktree wt-35 — MERGED + LIVE 2026-07-19 (a7872ba). New pure `compute_growth_rate`/`compute_lagging` in `ironlog/engine/stall.py`, `detect_stall` untouched. Opus-reviewed (routed per "when in doubt — review"): clean of Critical/High/Medium, one Low (type annotation) fixed directly by Tier A. 615 passed. No HUMAN GATE required.
- ~~.specs/36-movement-weakness-signal-model.md~~ → codex, worktree wt-36 — MERGED + LIVE 2026-07-19 (8dc0d4c). New append-only (NOT singleton, confirmed via an explicit multi-row test) `MovementWeaknessSignal` table + migration 034, mirroring `E1rmHistory`'s dual-FK DDL shape. Review-exempt (additive-only, spec-dictated tests). 617 passed. **[HUMAN GATE] satisfied at dispatch and merge.**
- ~~.specs/37-weak-point-signal-wiring.md~~ → gemini, worktree wt-37 — MERGED + LIVE 2026-07-19 (263cce4). Wired `detect_stall` into `run_analysis.py` for the FIRST time (previously exclusively a v0.6 generation-time consumer). **Opus review (routed, non-trivial new logic): clean of Critical/High/Medium** — two-pass rates-then-lagging structure, `db.add()` ordering before `apply_analysis`'s commit point, self-exclusion in the comparison set, and all three tests' math independently verified by hand. Three Low docstring/redundancy nits, cosmetic, not fixed. 620 passed. No HUMAN GATE required.
- ~~.specs/38-weak-points-endpoint.md~~ → codex, worktree wt-38 — MERGED + LIVE 2026-07-19 (8610299). `GET /weak-points`: latest-per-movement dedup + muscle-group rollup (primary muscle only). **Opus review (routed, real aggregation logic not pure CRUD): clean of Critical/High/Medium.** One Low fixed directly by Tier A (a test wasn't actually pinned to the `computed_at` comparison — insertion order happened to coincide; reversed the insertion order, re-verified still green, now genuinely proves the dedup logic). One Low noted (a latent `KeyError` if a signal ever references a deleted `Movement`, unlikely given movements are locked reference data). 624 passed. No HUMAN GATE required.

**All 4 specs in this batch are MERGED and LIVE as of 2026-07-19 (main @ 8610299, 624 tests passing). Real weak-point assessment feature is server-complete.**

Delegation ratio: 4/4 (100%)
Merge order: 35 → 36 → 37 → 38 (35/36 were parallel-safe by design but ended up merging serially in practice; 37/38 likewise — no conflicts at any point, confirmed disjoint files throughout, matching this session's now-repeated pattern).

Notes:
- **Spec 37's grounding correction mattered in practice**: the design doc assumed `detect_stall` was already being called somewhere reachable from `run_analysis.py` (mirroring how the readiness/goal-gate batches reused already-wired signals) — a pre-dispatch check found this was wrong; `detect_stall` was exclusively a generation-time consumer before this batch. The spec was corrected to make this explicit before dispatch, and the actual diff confirmed the correction was necessary (the module docstring's false "detect_stall is NOT called here" line had to be removed).
- **This batch's two non-schema Opus reviews (35, 37, 38 — three, not two) both found genuine, fixable Low findings, not just clean passes** — a recurring pattern worth noting: even "when in doubt, review" (not mandatory) passes on new-but-not-invariant-touching logic keep surfacing real, if minor, issues. The bar for skipping review on non-trivial logic should stay high.
- Client-side follow-on: none yet — server-only, no new client-visible surface beyond `GET /weak-points` (which nothing currently displays).

## 2026-07-20 batch: missed-workout handling (design approved via brainstorming, docs/superpowers/specs/2026-07-20-missed-workout-handling-design.md)

Second of the three remaining Phase-1 roadmap items (real weak-point assessment already shipped; a standalone cardio/interval day-type designed separately, no dependency between the two). Makes `ProgramDay.day_index` the first real consumer of the weekly-schedule anchor it was always meant to be — `/generate` remains entirely client-driven, unchanged, per the design's explicit "no new server-side scheduling resolver" decision.

- ~~.specs/39-missed-day-record-model.md~~ → codex, worktree wt-39 — MERGED + LIVE 2026-07-20 (25ecb61). New append-only (NOT singleton) `MissedDayRecord` table + migration 035, added to `ironlog/models/program.py`. Review-exempt (additive-only, spec-dictated tests). **[HUMAN GATE] satisfied at dispatch and merge.**
- ~~.specs/40-missed-day-detection-logic.md~~ → codex, worktree wt-40 — MERGED + LIVE 2026-07-20 (f67e145). New `ironlog/persistence/missed_days.py`. **Opus review (routed, non-trivial new logic): clean of Critical/High.** One Medium filed as an open follow-up, not fixed: detection is current-week-only, so a multi-day server-downtime gap wouldn't be backfilled — needs a real design decision about how far back to scan, narrow trigger condition. One Low fixed directly by Tier A (`# noqa: E712` on the `ProgramDay.is_rest == False` SQLModel comparison). 10 new tests, all edge cases from the spec covered. No HUMAN GATE required.
- ~~.specs/42-missed-days-endpoints.md~~ → codex, worktree wt-42 — MERGED + LIVE 2026-07-20 (bfe3840). `GET /missed-days` + acknowledge/reschedule. **Opus review (routed, worker-invented tests + non-trivial endpoint logic): clean of Critical/High**, confirmed N+1-avoidance in the batched `ProgramDay` fetch and confirmed 9/9 tests genuinely meaningful (not tautological). One Low fixed directly by Tier A (both write endpoints now look up `ProgramDay` — and 404 if missing — BEFORE mutating/committing status, so a missing `ProgramDay` can no longer mask an already-persisted mutation behind an error response). Two Low notes, not fixed: GET's silent skip-on-missing-`ProgramDay` vs. the write endpoints' 404 is an undocumented asymmetry; `GET /missed-days` has no `ORDER BY` (DB-unspecified list order). 645 passed. No HUMAN GATE required. (Generated by codex, left uncommitted; Tier A committed after verification per exception #7 — recurring pattern this session.)
- ~~.specs/41-missed-days-nightly-timer.md~~ → codex, worktree wt-41 — MERGED + LIVE 2026-07-20 (6bb6983). Standalone `scripts/check_missed_days.py` + systemd timer/service, mirrors `scripts/sync_withings.py`/`deploy/ironlog-withings-sync.*` exactly (03:30 vs. Withings' 03:00, to avoid clustering) — content cross-diffed line-for-line against the sibling files before commit. Review-exempt (pure deploy-infra, no application logic, faithful mirror of an already-reviewed pattern). 645 passed. No HUMAN GATE required. **Dispatch timed out at the consensus-mcp layer but the underlying `codex exec` process was still alive** — followed the standing timeout playbook (found the live PID, polled for natural exit rather than re-dispatching); process completed correctly ~several minutes later, left uncommitted, Tier A committed after verification per exception #7. **Process note: this spec was NOT actually dispatched earlier in this session despite an earlier self-correction claiming otherwise** — confirmed via `git log --all` (no commits, no worktree existed) before finally dispatching it here; this was purely a control-flow tracking error on Tier A's part mid-session, not a technical issue.

**All 4 specs in this batch are MERGED and LIVE as of 2026-07-20 (main @ 6bb6983, 645 tests passing). Missed-workout handling feature is server-complete.**

Delegation ratio: 4/4 (100%)
Merge order: 39 → 42 (dispatched/merged before 40 in practice, since it only depends on 39; both depend on 39 merged, confirmed disjoint files) → 40 → 41 (depends on 40 merged, dispatched last after an earlier tracking gap was caught and corrected).

Notes:
- **Only spec 39 required a HUMAN GATE** (DB schema change) — specs 40, 41, 42 hit no Forbidden-list item and proceeded on standing autonomous authorization once their dependencies cleared.
- **This is the second nightly-timer feature this session** (after Withings' `ironlog-withings-sync.timer`) — spec 41 mirrors that exact pattern (script shape, `.service` hardening directives, install procedure) rather than reinventing it. Confirmed the sibling timer's exact current file contents before writing the spec, not recalled from memory, and re-confirmed via a line-for-line diff after generation.
- **Recurring pattern continues**: all three non-schema specs in this batch (40, 41, 42) were codex-generated, correct, and fully tested, but left uncommitted — Tier A verified and committed each per exception #7. Same pattern noted in the 2026-07-10 batch's own notes; still unresolved at the dispatch-mechanism level.

**DEPLOYED 2026-07-20**: migration 035 applied (along with migration 034 from the prior weak-point-assessment batch, which turned out to be undeployed too), `ironlogv2.service` restarted (active-use check first, confirmed with user), smoke-tested (`GET /missed-days` → `200 []`). Nightly timer installed and enabled on myflix, verified with a manual run (`{'newly_missed': 0, 'resolved': 0}`). Full completion report: `~/project-ops/reports/2026-07-20-missed-workout-handling.md`.

**Post-deploy fix (2026-07-20, user-reported)**: Withings backup-reconciliation timer (`ironlog-withings-sync.timer`) ran at 03:00, BEFORE the athlete's typical ~7:30am weigh-in — since the webhook (not this timer) is the real-time primary sync path, this only mattered if a webhook silently failed, in which case the backup wouldn't catch that morning's miss until the NEXT night (~24h stale) instead of the same day. Moved to 09:00 (mechanical single-literal-value change, applied directly per exception #3; commit `6367e2f`). Redeployed and confirmed next run at 2026-07-21 09:10 EDT.
- Client-side follow-on: none yet — server-only, no new client-visible surface beyond `GET /missed-days` (which nothing currently displays).

## 2026-07-20 deferred follow-ups (investigated, per user request to tackle before the cardio/interval day-type)

Three items deferred earlier this session (see memory: `ironlog-v2-dips-progression-followup`, `ironlog-v2-gs1-split-bug-followup`, `ironlog-v2-rev-hyper-rep-target-followup`), investigated now at the user's request.

- **Dips progression**: investigated, **no bug found**. Dips is on an accessory/weak-point tier (confirmation_window=2, non-T1). Session 6 (2026-07-11, 150x12x3 ON_TARGET) brought the streak to 1. Session 11 (2026-07-19, athlete self-selected 155lbs, still hit 12 reps clean x3, ON_TARGET) brought the streak to 2, meeting the window — `earned_load_step=5.0` (from Dips' `increment_ladder=[5]`) is correctly staged as `MovementState.pending_load_delta=5.0`, confirmed via direct DB read. Per the engine's Option-C design, this applies to `current_load` at the NEXT session generation for this movement, not immediately — the next generated Dips session will prescribe 155lbs. No code or data change needed.
- **D6 GS1 split bug**: root cause confirmed via direct code read (`skeleton.py`/`assembler.py`) — GS1's Pull-up (`TierExercise` id=33) is seeded `tier_role="anchor"` inside a `GIANT_SET` tier, the ONLY such combination anywhere in the program (every other giant tier uses only free/semi roles). `lay_skeleton()` routes ALL anchor-role exercises into a separate `anchor_movement_ids` list unconditionally, and `assembler.py` gives every anchor its own `STRAIGHT` group — bypassing the giant-set grouping mechanism entirely regardless of the anchor's own tier's `tier_kind`. User confirmed intent: Pull-up should physically rotate with Dips + Hip Thrust in one true 3-exercise giant set. **Spec written**: `.specs/43-gs1-anchor-in-giant-tier-fix.md` — real logic fix (skeleton.py's anchor/adaptive routing + `_slot_kind`'s giant-vs-accessory classification), routed through the standard dispatch pipeline below (not mechanical — touches candidate-menu eligibility / LLM-substitution guardrails, a real invariant).
- **Reverse Hyper / Reverse Hyper Recovery fixed rep/load**: investigated and applied directly (2026-07-20), backup-first (`ironlog.db.bak-20260720-182418`). Mechanism confirmed: `MovementState.active_rule="FIXED_LOAD"` is the established non-progressing pattern (precedent: "Light Reverse Hyper", movement_id=20, already using it — untouched per explicit instruction). `calibration_status`/`confirmed_at` don't gate prescription (confirmed via `load_trust.py::compute_load_trust` — only `current_load` presence/recency does). Applied: Reverse Hyper (movement_id=19) `TierExercise.rep_low` 8→12 (both D2/D5 `MovementState` rows' `active_rule` REP_LADDER→FIXED_LOAD — functionally already a no-op via an empty `rep_ladder`, but FIXED_LOAD makes it explicit/robust rather than an accidental empty-config no-op; `current_load` unchanged at 180.0, already correct). Reverse Hyper Recovery (movement_id=116) `TierExercise.rep_low` 15→20; `MovementState.current_load` NULL→90.0, `calibration_status` INHERITED→MEASURED (matching Light Reverse Hyper's precedent); `active_rule` already FIXED_LOAD. Data-only change, live immediately (no service restart needed — SQLModel reads current DB state per request). Deferred-followup memory entries removed — all three items (Dips, GS1, Reverse Hyper) now resolved.

### Spec 43 routing
- `.specs/43-gs1-anchor-in-giant-tier-fix.md` → codex, worktree wt-43, depends on: none. Single self-contained fix, 2 files (`ironlog/generation/skeleton.py` modified, `tests/test_generation_skeleton.py` modified + new tests). No schema change, no Forbidden-list hit. **Opus review mandatory regardless of diff cleanliness** — touches candidate-menu/LLM-substitution eligibility, a real generation invariant (same standard as the note-resolver and Withings-sync-logic specs earlier this session).

Delegation ratio: 1/1 (100%)
Merge order: wt-43 standalone.

## 2026-07-20 batch: standalone cardio/interval day-type (design approved via brainstorming, docs/superpowers/specs/2026-07-20-cardio-interval-day-design.md)

Third and final Phase-1 roadmap item (real weak-point assessment and missed-workout handling both already shipped). Log-only, no generation, no progression engine, no ProgramDay/day_role involvement, not tracked by missed-workout-handling (falls out for free since `check_missed_days()` already excludes `is_rest=true` days, and the two candidate slots — day_index 3/7 — already are).

- ~~.specs/44-cardio-log-model.md~~ → codex, worktree wt-44 — MERGED + LIVE 2026-07-21 (9d82633). New standalone `CardioLog` table + migration 036, added to `ironlog/models/library.py`. Confirmed empirically: a bare `date: date` field (no assigned `Field(...)`) does NOT hit this file's known pydantic naming clash (the `_Date` alias workaround, established by `DailyReadiness`, was not needed here). Review-exempt (additive-only, spec-dictated tests). 650 passed. **[HUMAN GATE] satisfied at dispatch and merge.**
- ~~.specs/45-cardio-log-endpoints.md~~ → codex, worktree wt-45 — MERGED + LIVE 2026-07-21 (47406f5). `POST/GET /cardio-log` + `GET /cardio-log/weekly-summary`, mirrors the established `GET/POST /goals` pattern exactly (simple CRUD, no join complexity — CardioLog has no FKs). Reuses missed-workout-handling's exact Monday-start week-boundary formula verbatim. Review-exempt (mirrors an already-reviewed pattern, no novel logic) — worker-invented tests independently verified non-tautological (the weekly-summary test monkeypatches a fixed `date.today()` and proves a Sunday-dated log doesn't count toward the following Monday's week). 653 passed. No HUMAN GATE required.

**Both specs in this batch are MERGED and LIVE as of 2026-07-21 (main @ 22b5b61, 653 tests passing). Standalone cardio/interval day-type feature is server-complete — the third and final Phase-1 roadmap item now has server-side support.**

Delegation ratio: 2/2 (100%)
Merge order: 44 → 45.

**`/verify-plan` run 2026-07-21**: PASS. No parallel pairs to check (44→45 is sequential, not parallel) — zero file-target overlap regardless (44: `library.py`/`__init__.py`/migration 036/its own test; 45: `schemas_cardio_log.py`/`app.py`/its own test). Boundary pass: spec 44 hits the Forbidden list (DB schema change) → **HUMAN GATE** required at dispatch and merge; spec 45 hits nothing → clear to proceed once 44 merges. Safe to run `/route-plan`.

Notes:
- **Only spec 44 requires a HUMAN GATE** (DB schema change) — spec 45 hits no Forbidden-list item and can proceed on standing autonomous authorization once its dependency clears.
- **Deliberately decoupled from ProgramDay/day_role** — unlike every other program-generation feature this session, this one has zero interaction with `lay_skeleton`, `/generate`, `MovementState`, or the progression engine. The two empty `ProgramDay` slots (day_index 3/7, `is_rest=true`) that map conceptually to the athlete's real 2x/week Z2 rhythm are left completely untouched — this feature doesn't read or write them at all.
- **Client-side follow-on** (IronLog-V2-Client, separate repo, own `.specs/`): Today-screen weekly rollup + tappable log-entry form + a history list reusing the existing `history` screen pattern. Not specced yet — verify the real endpoint shapes (`CardioLogOut`, `CardioWeeklySummaryOut`) via a live curl before dispatching, matching this session's established pattern for client follow-ons on server-first features.
- No client interval-timer involvement — this is Z2 steady-state, not interval work; `IntervalTimerService` stays scoped to finishers.

## 2026-07-22 batch: HT performed-floor reconciliation (design approved via brainstorming, docs/superpowers/specs/2026-07-22-ht-performed-floor-reconciliation-design.md)

Real code fix for the third occurrence tonight of "self-selected deviation from prescription gets silently ignored" — this time for HT (band-composite Hip Thrust), fixable using data already captured (`SetLog.felt_peak`), no new client capture needed. The parallel assist-ladder case (Nordic Curl/Knee Raise) needs a genuinely new capture field and is explicitly deferred to a separate design.

- `.specs/47-ht-performed-floor-reconciliation.md` → codex, worktree wt-47, depends on: none. Touches `ironlog/engine/band_composite.py` (new `resolved_band_config`/`ht_performed_floor`), `ironlog/persistence/ht_refine.py` (behavior-preserving refactor to use the shared helper), `ironlog/generation/assembler.py` (the reconciliation point). No schema change, no Forbidden-list hit.

Delegation ratio: 1/1 (100%)
Merge order: wt-47 standalone.

Notes:
- No HUMAN GATE — pure code fix, no schema/auth/API-surface change. Class 1 restart at deploy.
- **Opus review mandatory regardless of diff cleanliness** — touches HT's generation-time setup resolution, a real invariant this codebase has been burned by before (the HT-override compounding bug, the calibration guidelines' own standing caution about HT changes). Same standard as the note-resolver and Withings-sync-logic specs earlier this session.
- `ht_refine.py`'s `_resolved_band_config` refactor must be verified as behavior-preserving (every existing `test_ht_refine.py` test passes unchanged) — this is a refactor-while-fixing-something-else, worth explicit scrutiny at review since it's easy to accidentally change the 3-tier fallback priority while "simplifying" it.
- Out of scope, deferred to a future design: assist-ladder reconciliation (Nordic Curl, Face-Up Incline Knee Raise, Reverse Nordic Curl) — needs new client capture (an "actual assist level used" field), a materially bigger lift than this spec.
